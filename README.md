# @conscendotech/siid-compression-api

> **Auto-generated mirror.** The type files in this repo are synced from
> [`Conscendotechnologies/Siid`](https://github.com/Conscendotechnologies/Siid) →
> `extensions/siid-compression/api/` by the workflow in `.github/workflows/sync.yml`.
> **Do not edit `index.d.ts` / `siid-compression.d.ts` here** — edit them in the source repo; the
> next sync overwrites local changes.

Public TypeScript types for the **SIID Compression** VS Code extension
(`ConscendoTechInc.siid-compression`). Types only — the live API is bound at runtime via the
installed extension's `activate()`.

## Install (external consumers, e.g. SIID-Code)

```bash
pnpm add github:Conscendotechnologies/siid-compression-api
# pin a tag: pnpm add github:Conscendotechnologies/siid-compression-api#v0.1.0
```

```ts
import type { ICompressionApi } from '@conscendotech/siid-compression-api'
```

The full usage guide lives in the synced `README.md` (from the source repo) once the first sync runs.
